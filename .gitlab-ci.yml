
codecakebuilder:
  tags:
    - windows
  script:
    - dotnet run --project CodeCakeBuilder -nointeraction
  artifacts:
    paths:
      - '**\Tests\**\TestResult*.trx'
      - '**Tests\**\Logs\**\*'
    when: always
    expire_in: 6 month
