{
  "private": true,
  "scripts": {
    "build": "tsc && vite build",
    "start": "vite serve",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@signature/ck-observable-domain": "workspace:*",
    "@signature/ck-observable-domain-mqtt": "workspace:*",
    "@signature/ck-observable-domain-signalr": "workspace:*",
    "axios": "^1.5.1"
  },
  "devDependencies": {
    "@local/ck-gen": "workspace:*",
    "rimraf": "^3.0.2",
    "typescript": "5.2.2",
    "vite": "4.4.11"
  }
}
